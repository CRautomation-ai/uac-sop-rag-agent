# Python deps for Vercel serverless (api/index.py).
# Mirrors backend/requirements.txt so query + Neon + OpenAI work.
fastapi==0.104.1
openai>=1.12.0
psycopg2-binary==2.9.9
pydantic==2.5.0
python-multipart==0.0.6
