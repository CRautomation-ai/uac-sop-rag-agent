# Python deps for Vercel serverless (api/index.py).
# Only query path; document loading is lazy-imported so PyPDF2/python-docx not needed.
fastapi==0.104.1
openai>=1.12.0
psycopg2-binary==2.9.9
pydantic==2.5.0
python-multipart==0.0.6
PyJWT>=2.8.0
